@using Entity
@{
    ViewBag.Title = "ZiyaretciListele";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
    List<User> users = Query.UserList();
}

<div class="content">
    <div class="container-fluid">
        <div class="row">
            <div class="col-md-12">
                <div class="card">
                    <div class="card-header card-header-primary">
                        <h3 class="card-title ">ZİYARETÇİLER</h3>
                    </div>
                    <div class="card-body">
                        <div class="table-responsive">
                            <table class="display" style="width:100%" id="categoryTable">
                                <thead class=" text-primary">
                                    <tr>
                                        <th>Kullanıcı Adı</th>
                                        <th>Ad</th>
                                        <th>Soyad</th>
                                        <th>E-mail</th>
                                        <th>Şifre</th>
                                        <th>Telefon</th>
                                        <th>İşlemler</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (User user in users.Where(x => x.Role == Role.Ziyaretçi))
                                    {
                                    <tr>
                                        <td>@user.UserName</td>
                                        <td>@user.FirstName</td>
                                        <td>@user.LastName</td>
                                        <td>@user.Email</td>
                                        <td>@user.Password</td>
                                        <td>@user.PhoneNumber</td>
                                        <td>
                                            <a href="/Admin/User/ZiyaretciGüncelle/@user.ID">
                                                <button type="button" class="btn btn-info">GÜNCELLE</button>
                                            </a>
                                            <a href="/Admin/User/ZiyaretciSil/@user.ID">
                                                <button type="button" class="btn btn-danger">SİL</button>
                                            </a>
                                        </td>
                                    </tr>
                                    }
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
